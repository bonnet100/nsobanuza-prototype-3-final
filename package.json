{
  "private": true,
  "name": "nsobanuza",
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "client",
    "frontend"
  ],
  "scripts": {
    "server": "npm --prefix backend run server",
    "dev": "npm --prefix client run dev",
    "dev:full": "concurrently \"npm run server\" \"npm run dev:client\"",
    "dev:client": "npm --prefix client run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
